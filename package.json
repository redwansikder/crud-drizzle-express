{
  "name": "crud-drizzle-express",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.js",
    "start": "node --env-file=.env src/server.js",
    "generate": "node --env-file=.env ./node_modules/drizzle-kit/bin.cjs generate",
    "migrate": "node --env-file=.env src/db/migrate.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.5"
  }
}
